// This component should either be removed completely or modified to remove household functionality
// If you want to keep the component for future use but disable its functionality:

import React from 'react';

const HouseholdInvite = () => {
  return (
    <div>
      {/* Remove household invitation functionality */}
      {/* Could replace with a message about collaboration */}
      <p>All users automatically collaborate on the same data.</p>
    </div>
  );
};

export default HouseholdInvite;
